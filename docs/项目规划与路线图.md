# DaHua_CGI 项目规划与路线图整理

> 整理自远端 `/data/DaHua_CGI` 的规划文档(`ROADMAP.md / TASKS.md / CHANGELOG.md / DEVELOPMENT_GUIDE.md / docs/tasks/task10-template-and-query.md` 等)与本地源码对照。

## 一、规划文档地图
**项目内(本地与远端同源)**
| 文档 | 作用 |
|---|---|
| `ROADMAP.md` | 版本路线图 + 里程碑 |
| `TASKS.md` | 任务列表(进行中/待办/已完成),含分项与测试基线 |
| `CHANGELOG.md` | 版本变更史(V9.5→V9.7-alpha),roadmap 明细 |
| `docs/DEVELOPMENT_GUIDE.md` | **项目协作总纲**(Hermes 总调度 + Claw-Claude/Claw-CodeX/OpenClaw 分工、开发铁律) |
| `docs/tasks/task10-template-and-query.md` | **Task10 规划**(Excel 导入模板 + CGI 批量查询) |
| `docs/api/` | 6 份模块接口文档(async_executor/device_manager/log_manager/config_manager/cgi_reference_manager + README) |
| `src/README.md` | 实现说明(源于 V9.5 工作副本) |
| `task_webui_build.md` / `task_gui_upgrade.md` / `task_github_push.md` / `docs/Task9_spec.md` | 单任务派工单 |

**远端其它应用**(同在 `/data/`,属 Hermes 生态,非 Dahlua 主项目)
- `hermes/`、`hermes-webui/`(各自 `ROADMAP.md`/`SPRINTS.md`/`AGENTS.md`)、`github-logging-project/`、`user-api/`、`parking2.0.0/` 等。

## 二、总体路线图
| 版本/任务 | 内容 | 状态 |
|---|---|---|
| **V9.5** (05-22) | 项目初始化、venv、Git、异步执行器骨架 | ✅ |
| **V9.6-alpha** (06-01~06-07) | Task1 配置重构 / Task2 异步执行器优化 / Task3 异常+日志 / Task4 代码清理 / Task5 覆盖率>80% / Task6 API文档+集成测试 / Task7 性能 / Task8 CGI命令扩展 / Task9 多设备并发+聚合报表;GUI 聚合报表 Tab;Web UI 构建 | ✅ 全量 329/329 |
| **V9.7-alpha** (07-10) | 类型注解修正、消除 `except:pass`、Excel 读 sheet 修复、Web 模板路径修复 | ✅ |
| **Task10** | Excel 导入模板+严格校验、CGI 批量查询(`cgi_query.py` + `/query` 页 + 导出) | **代码已实现**,未见规划勾选 |
| **根目录待办** | 推送远程仓库 / 配置远程 Git 仓库 | ⏳ 未完成 |
| **V10.0** (未启动) | 架构升级:重构、多设备并发、Web 界面管理 | ⏳ 未开始 |

## 三、与当前工作(Web UI 布局)对应
- Web UI 由 `task_webui_build.md` 规划(设备/配置/报表/配置管理/日志 5 页),后补 CGI 查询(`task10-template-and-query.md` 需求2)。
- 近期「6 大页面布局统一为 `panel-box/ops-panel` 新框架 + 本地默认 5001」即对这套 Web UI 规划的收尾打磨。
- 对照 Task10 验证标准,`/query`、`/api/query/execute`、`/api/query/export`、模板下载等**均已存在**,基本完成。

## 四、规划收尾建议
1. 将已实现的 **Task10**(CGI 查询 + 模板)在 `TASKS.md` / `ROADMAP.md` 正式勾选,并记入 `CHANGELOG.md`。
2. 本地 `src/web`(最新布局)与远端 `/data/DaHua_CGI` dev 分支有差异,建议回同步远端再统一提交,推进「推送远程仓库」待办。
3. 引入 `dahua-web-tool_对比学习.md` 中的亮点(见同目录文档):SSE 流式批量、请求级日志上下文、操作审计、移动端响应式。
